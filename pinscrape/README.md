# pinscrape with a script

### usage:
```sh
python pinscrapescript.py -h # prints out help

# examples
python pinscrapescript.py -k 'trees' -l 10 -o 'output'
# -k is key
# -l is limit
# -o is output
```